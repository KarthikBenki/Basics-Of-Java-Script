//Numbers and Math Object

const num1 = 100;
const num2 = 50;
//basics of numbers
let val;
val = num1 + num2;
val = num1 - num2;
val = num1 * num2;
val = num1 / num2;
val = num1 % num2;

//Math object: in built object of java script
val = Math.PI;
val = Math.round(2.3)
val = Math.ceil(2.3) //will assign next value
val = Math.floor(2.9)//will assign lesser value
val = Math.sqrt(169)
val = Math.abs(-3)//will give u 3
val = Math.pow(8,3)//8 power 3
val = Math.min(3,4,2,1,4,5,0)
val = Math.max(3,4,2,1,4,5,0)
val = Math.random()*20
val = Math.floor((Math.random()*20)+1)

console.log(val);